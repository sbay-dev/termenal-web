{
  "name": "termenal-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Arabic / RTL terminal for the browser.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b",
    "server": "npm start -w @termenal-web/terminal-server",
    "terminal": "npm run dev -w @termenal-web/demo"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  }
}
